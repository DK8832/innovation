# v0.21.0 — Qwen3 빈 답변 수정·APK Builder

원본 폴더: `CERTI_ON_OGQ_STANDALONE_AI_ALL_FIXED_V6`

- `think=false` + `/no_think`
- 빈 chat 응답 재시도 후 `/api/generate` fallback
- `<think>` 제거
- `MAKE_FLUTTER_APK.bat` 추가
