"""외부 데이터 소스 연동 계층.

설계 원칙 (실측으로 정한 것):
1. 공공 API가 1순위다. 주관처 사이트 직접 크롤링은 실측 결과 신뢰할 수 없다
   (큐넷은 JS가 표를 조립하고, 한국사 사이트는 봇을 차단한다 — docs/DATA_SOURCES.md 참고).
2. API 키가 없어도 서비스는 완전히 동작한다. 큐레이션된 exams.json이 항상 기본값이고,
   공공 API는 그것을 "갱신·대조"하는 보조 수단이다.
3. 수집한 값을 exams.json에 자동으로 덮어쓰지 않는다. 반드시 검토 큐를 거친다.
   틀린 날짜는 수험생이 시험을 놓치게 만들기 때문이다.
"""
